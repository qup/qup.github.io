# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Qup
email: janegil@qup.no
description: >- # this means to ignore newlines until "baseurl:"
  Company; Qup DA. 
  Industry; Sales - Web design and implementation, computer programming, consulting services.
  Founders; Jan Egil Staff Øvretveit, Casper Beyer
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://qup.no" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: janstovr
github_username:  janstovr
layouts_dir:     _layouts
includes_dir:    _includes
defaults:
  - scope:
      path: "assets/images"
    values:
      image: true
include:
  - assets/js
  - assets/css
sass:
  sass_dir: assets/_scss/
  style:    :compressed

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
